Add an ErrorHandler class to manage type errors. As an example, the compiler must show an error message for the input.txt
file, indicating the line and column numbers. The rest of type errors will be handled in the following laboratories.

